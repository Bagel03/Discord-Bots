const { pathToFileURL } = require("url")

module.exports.importES6andCJS = (path) => {
    const module = import(pathToFileURL(path));
    if(module.default) return module.default;
    return module;
}